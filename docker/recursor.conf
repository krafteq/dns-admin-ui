# PowerDNS Recursor configuration

# DNS listener
local-address=0.0.0.0
local-port=53

# API
api-key=changeme
webserver=yes
webserver-address=0.0.0.0
webserver-port=8082
webserver-allow-from=0.0.0.0/0,::/0

# Allow queries from anywhere (for dev)
allow-from=0.0.0.0/0,::/0

# Logging
loglevel=4
